# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - pa200-alexh

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to registry
      uses: docker/login-action@v2
      with:
        registry: https://pa200alexh.azurecr.io/
        username: ${{ secrets.AzureAppService_ContainerUsername_f1be5f1260c04990b3e0396c052bd0c3 }}
        password: ${{ secrets.AzureAppService_ContainerPassword_19b84adaca464c7a879b820346914103 }}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v3
      with:
        push: true
        tags: pa200alexh.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_f1be5f1260c04990b3e0396c052bd0c3 }}/benchstore/azure:${{ github.sha }}
        file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'pa200-alexh'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_b52ae98d714c48d9b37e02babaaaf5b0 }}
        images: 'pa200alexh.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_f1be5f1260c04990b3e0396c052bd0c3 }}/benchstore/azure:${{ github.sha }}'